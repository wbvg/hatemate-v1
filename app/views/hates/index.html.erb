<div class="hero-unit">
<h1>The Top Hates</h1>

<%= @hates %>
<!% @hates.each do |hate| %>

<!%= link_to(hate.user.username, hate) %>

<!% end %>

<!% @hates.each do |hate| %>
    <ul>
      <li><!%= link_to(hate.user_id.username, hate) %></li>
        <li><!%= image_tag(hate.user.image)%></li>
      <li><!%= hate.category %></li>
      <li><!%= hate.post %></li>
      <li><!%= hate.hate_rate %></li>
    </ul>

<!% end %>
</div>